# Test + lint tooling. Kept out of requirements.txt so the Vercel function
# bundle stays limited to what the engines actually import at runtime.
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
ruff==0.16.3
